name: Supervisor Brain Verdict Stall Repair

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/supervisor-brain-verdict-stall-repair-temp.yml
      - .github/scripts/deploy-brain-verdict-stall-repair.ps1

permissions:
  contents: read

jobs:
  repair:
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        slot: [1,2,3,4]
    runs-on: self-hosted
    timeout-minutes: 12
    steps:
      - name: Checkout exact main
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Repair only DESKTOP-4K7IM13
        shell: cmd
        run: |
          @echo off
          echo SLOT=${{ matrix.slot }}
          echo MACHINE=%COMPUTERNAME%
          if /I not "%COMPUTERNAME%"=="DESKTOP-4K7IM13" (
            echo TARGET_MATCH=False
            exit /b 0
          )
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File ".github\scripts\deploy-brain-verdict-stall-repair.ps1" -TargetComputer "DESKTOP-4K7IM13"
          exit /b %ERRORLEVEL%
